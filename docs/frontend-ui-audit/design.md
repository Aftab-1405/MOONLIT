# Canonical Design Reference

The sole authoritative design specification for Moonlit is:

`front-end/DESIGN.md`

That file is immutable for the current frontend design-compliance project. This pointer does not
duplicate, reinterpret, or extend the design system.

The current specification is dark-canvas only. Any future light theme requires a separately
approved update to the canonical specification before implementation.
